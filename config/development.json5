{
  api: {
    url: "http://localhost:4000",
    port: 4000,
    secret: "123fdce931dab002d05abaa1cf1a1724", // To be overridden in production.
  },
  app: {
    url: "http://localhost:3000",
    port: 3000, // This is tied to our OAuth providers' callback URIs, so OAuth will break if you change it.
  },
  db: {
    port: 27020, // Synchronize this with the one in docker-compose.yml.
  },
  redis: {
    port: 6370, // Synchronize this with the one in docker-compose.yml.
  },
}
